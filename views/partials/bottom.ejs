<style>
    * {
        margin: 0;
        padding: 0;
    }

    @import url('https://fonts.googleapis.com/css2?family=Alegreya+Sans:ital,wght@0,300;0,400;0,500;0,700;1,300&display=swap');
    @import url('https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;1,100;1,200&display=swap');

    /* body{
        background-color:rgba(5, 12, 43, 0.829);
    } */
    .bottomNav {
        position: fixed;
        bottom: 0;
        width: 100%;
        height: 70px;
        display: flex;
        justify-content: center;
        left: 0;
        align-items: center;
    }

    .header_txt {
        font-family: 'Poppins', sans-serif;
    }

    .head_nav {
        display: flex;
        align-items: center;
        height: 70px;
        justify-content: space-around;
        width: 100%;
    }

    .head_nav h1 {
        font-size: 18px;
    }

    .head_nav img {
        width: 50px;
        height: 50px;
        border-radius: 50px;
        object-fit: cover;
    }

    main {
        position: relative;
    }

    .create_div {
        position: fixed;
        bottom: 100px;
        float: right;
        width: 100%;
        display: flex;
        justify-content: right;
    }

    .create_div button {
        background-color: #09103b;
        color: white;
        font-size: 18px;
        padding: 10px;
        border-radius: 50px;
        display: flex;
        height: 40px;
        width: 40px;
        align-items: center;
        justify-content: center;
        outline: none;
        border: none;
        margin-right: 20px;
        cursor: pointer;
        box-shadow: 4px 4px 10px 0px rgba(0, 0, 0, 0.45);
        -webkit-box-shadow: 4px 4px 10px 0px rgba(0, 0, 0, 0.45);
        -moz-box-shadow: 4px 4px 10px 0px rgba(0, 0, 0, 0.45);

    }

    /* font-family: 'Poppins', sans-serif; */

    .bottomNav ul {
        width: 90%;
        max-width: 500px;
        display: flex;
        justify-content: space-around;
        position: relative;
        align-items: center;
        height: 80%;
        border-radius: 20px;
        background-color: #e9eaf0;
        padding: 0;
        margin: 0;
    }

    .head_nav{
        box-shadow: 0px 0px 10px 1px rgba(0, 0, 0, 0.055);
    }
    .bottomNav ul a p {
        font-size: 14px;
        font-weight: normal;
        padding: 0;
        margin: 0;
    }

    svg {
        fill: none;
    }

    .active:is(:link, :active, :visited).active {
        color: #3880ff;
    }

    .fa-solid {
        font-size: 20px;
    }

    .active .fa-solid {
        color: #3880ff;
    }

    .bottomNav ul a {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: 2px;
        text-decoration: none;
        /* color: #666666; */
        color: #09103b;
        font-family: 'Alegreya Sans', sans-serif;
    }
</style>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css"
    integrity="sha512-KfkfwYDsLkIlwQp6LFnl8zNdLGxu9YAA1QvwINks4PhcElQSvqcyVLLD9aMhXd13uQjoXtEKNosOWaZqXgel0g=="
    crossorigin="anonymous" referrerpolicy="no-referrer" />
    <div class="create_div">
        <button data-bs-toggle="modal" data-bs-target="#addModal">
            <i class="fa-solid fa-add"></i>
        </button>
    </div>
    <!-- modal add task -->
    <div class="modal fade" id="addModal" tabindex="-1" aria-labelledby="addModalLabel"
                aria-hidden="true">
                <div class="modal-dialog">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="addModalLabel">Add Task</h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                        </div>
                        <!-- The modal body -->
                        <div class="modal-body">
                            <form>
                                <div class="mb-3">
                                    <label for="title-name" class="col-form-label">Title:</label>
                                    <input type="text" class="form-control add_task-title" id="add_title-name" name="title">
                                </div>
                                <div class="mb-3">
                                    <label for="date-time" class="col-form-label">Date:</label>
                                    <input type="date" class="form-control add_task_date" id="add_date"
                                        name="date-date">
                                </div>
                                <div class="mb-3">
                                    <label for="date-time" class="col-form-label">Time:</label>
                                    <input type="time" class="form-control add_task_time" id="add_time"
                                        name="date-time">
                                </div>
                                <div class="mb-3">
                                    <label for="summary-text" class="col-form-label">Summary:</label>
                                    <textarea class="form-control add_task_summary" id="add_summary-text"
                                        name="summary-text"></textarea>
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary"
                                        data-bs-dismiss="modal">Close</button>
                                    <button type="button" class="btn btn-primary" id="addBtn">Save</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
<div class="bottomNav">
    <ul>
        <a href="/home">
            <i class="fa-solid fa-house-chimney"></i>
            <p>Home</p>
        </a>

        <a href="/logout">
            <i style="color: rgb(180, 5, 5);" class="fa-solid fa-arrow-right-from-bracket"></i>
            <p>Logout</p>
        </a>
    </ul>
</div>
<script>
    const addBtn = document.querySelector('#addBtn');
    addBtn.addEventListener('click', (e) => {
        console.log('add data')
        e.preventDefault();
        const add_task_title = document.querySelector('.add_task-title').value;
        const add_task_date = document.querySelector('.add_task_date').value;
        const add_task_time = document.querySelector('.add_task_time').value;
        const add_task_summary = document.querySelector('.add_task_summary').value;
        const data = { add_task_title, add_task_date,add_task_time, add_task_summary };

        try {
            fetch('/addTask', {
                method: 'POST', // or 'PUT'
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify(data),
            })
                .then(response => response.json())
                .then(data => {
                    data.message == 'successfull'? location.reload() : alert('An Error Occured')
                    console.log('Success:', data);
                })
                .catch((error) => {
                    console.error('Error:', error);
                });

        } catch (error) {
            console.log(error)
        }
    })
</script>
<script>
    const links = document.querySelectorAll('a');
    const activePath = window.location.pathname;
    links.forEach((link) => {
        if (link.href.includes(`${activePath}`)) {
            console.log(activePath);
            link.classList.add('active');
        }
    })
</script>